# Модерация: кто что может и как этим пользоваться

## Роли

| Роль | Метка | Что может |
|---|---|---|
| **Владелец** | 👑 владелец | Всё. Первый администратор по дате регистрации |
| **Администратор** | ⚡ админ | Всё то же: роли, пароли, жалобы, удаление, ограничения |
| **Модератор** | 🛡 модератор | Жалобы, удаление чужих записей, ограничения |
| **Редактор** | ✎ редактор | Только данные сайта: итоги VS, альянсы, хронология, тексты |
| Участник | нет | Писать, комментировать, реагировать, жаловаться |

Участник метки не получает вовсе. Если подписать всех, подпись перестаёт
значить что-либо: она нужна, чтобы отличить того, кто отвечает за сайт.

**Владелец и администратор — одна роль в базе (`admin`).** Права одинаковые,
и делить их на уровни было бы неправдой: оба могут всё. Разница только
в подписи, и вычисляется она как «чья запись с ролью admin самая ранняя» —
не хранится полем, которое пришлось бы поддерживать руками.

**Редактор — не роль, а отдельный признак.** Правит историю сервера, но жалоб
и ограничений не видит. Это не упущение: вносящий итоги VS не обязан
разбирать споры, а модератор не обязан иметь доступ к истории. Слепить
их вместе значило бы выдавать лишнее, чтобы дать нужное.

## Как добавить человека

Порядок один для всех ролей: **человек сначала регистрируется сам**, потом
получает права. Иначе давать было бы некому.

### Редактор — нажатием в панели

1. Человек регистрируется на сайте, вкладка **Форум**.
2. Говорит вам свой ник.
3. Панель → **Игроки** → напротив ника **«Сделать редактором»**.

Всё. Он входит в панель тем же ником и паролем и правит данные сайта.
GitHub ему не нужен.

Снимается той же кнопкой — «Убрать из редакторов».

### Модератор или администратор — запросом в базу

Кнопки для этого нет намеренно. Роль даёт власть над людьми: удалять чужие
записи, закрывать возможность писать. Такое решение не должно приниматься
случайным нажатием рядом с обычной кнопкой — и должно требовать доступа
к базе, а не только к панели.

Supabase → **SQL Editor** → **New query**, подставить ник:

```sql
-- Модератор: жалобы, удаление чужих записей, ограничения.
update public.forum_users set role = 'moderator' where lower(nick) = lower('НИК');

-- Администратор: то же плюс роли, пароли и права редактора.
update public.forum_users set role = 'admin' where lower(nick) = lower('НИК');

-- Снять роль, вернуть в участники.
update public.forum_users set role = 'member' where lower(nick) = lower('НИК');
```

Должно появиться `Success. 1 row(s) affected`. Если `0 rows` — ник не совпал,
посмотрите написание в **Table Editor** → `forum_users`.

**После смены роли человек должен выйти и войти заново**: роль читается
при входе, в открытой сессии останется прежняя.

Три правила, которые база держит сама и обойти их нельзя:

- роль меняет только администратор — иначе модератор назначил бы админом себя;
- администратора нельзя забанить — это способ отобрать форум у владельца;
- нельзя снять роль с последнего администратора — форум остался бы без хозяина.

## Удалить чужой пост

**На сайте, а не в панели.** Панель показывает жалобы; удалять удобнее там,
где вы читаете.

1. Откройте пост на форуме.
2. Внизу поста → **«Удалить»**.
3. Выберите **пункт правил** и при желании напишите пояснение.
4. **Удалить**.

Пункт обязателен, и это не формальность. Автор увидит не «удалено»,
а «удалено по пункту 4: Без рекламы и торговли» — единственное, чем правило
вообще чему-то учит.

**Пост не исчезает.** На его месте остаётся заглушка с причиной. Молча
пропавший пост читается как поломка сайта и порождает второй такой же.

Комментарии удаляются так же — кнопка «Удалить» под каждым.

Свои записи автор удаляет сам, без выбора пункта: на месте появится
«Удалено автором».

## Разобрать жалобу

Панель → **Жалобы**. Видно, на какой пункт жалуются, кто пожаловался и текст
самой записи — переходить на сайт не нужно.

Два решения:

**«Удалить по пункту»** — убирает запись с той причиной, на которую жаловались,
и закрывает жалобу.

**«Оставить как есть»** — закрывает жалобу, ничего не удаляя. Нужно, когда
жалоба не по делу.

Порядок внутри важен: сначала удаляется запись, потом закрывается жалоба.
Наоборот было бы опасно — если удаление не пройдёт, нарушение останется
на сайте, а жалоба на него исчезнет из списка.

**Кто пожаловался, видно только модерации.** Открытый список жалующихся сам
становится инструментом травли.

## Тишина и запрет

Панель → **Игроки** → **«Запретить писать»**.

| Мера | Что значит | Когда снимается |
|---|---|---|
| **Тишина** | Не может писать, комментировать и реагировать. Остаётся на форуме, видит причину | Сама, по истечении срока |
| **Запрет** | То же, но без срока | Только вручную |

Сроки тишины: сутки, 3 дня, неделя, месяц.

Пункт правил обязателен и здесь: человек увидит причину, а не просто
«вам запрещено писать».

**Тишина стоит впереди запрета в списке намеренно** — чтобы первая мера
оказывалась соразмерной. Запрет без срока это последнее средство, а не
первое.

Снять всё: та же кнопка → **«Снять все ограничения»**.

Ограничения проверяет база, а не страница. Забаненный не сможет написать,
даже отправив запрос мимо сайта.

## Сбросить пароль

Панель → **Игроки** → **«Сбросить пароль»**.

Придумайте пароль или нажмите **«Придумать за меня»** — предложится
что-то вроде `мост-рек-42`: такое можно передать голосом, в отличие
от `xK7#pQ2z`.

**Пароль показывается один раз.** Сохранённого пароля не существует ни у кого:
база хранит необратимый отпечаток, а не сам пароль. Подсмотреть старый нельзя
даже вам — передайте новый человеку сразу.

Письма «восстановить пароль» не существует, потому что почты у сайта нет.
Это осознанный выбор: на бесплатном тарифе отправка писем зажата до считанных
штук в час, и подтверждение почты стало бы пробкой на регистрации.

## Чего не может никто

- **Стереть пост из базы.** Только пометить удалённым с причиной.
- **Изменить чужой текст.** Можно удалить, но не переписать: подмена чужих
  слов хуже удаления, потому что незаметна.
- **Сменить ник.** Ни свой, ни чужой: ником подписаны все посты, и смена
  оставила бы старые записи под прежним именем.
- **Забанить администратора.**
- **Выдать себе роль или снять себе запрет.**

Всё это держит база, а не страница. Проверки в браузере обходятся запросом
мимо сайта — там они нужны для понятных сообщений, а не вместо защиты.

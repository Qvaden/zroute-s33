-- ПЕРВЫЙ АДМИНИСТРАТОР.
--
-- Запускать ОДИН РАЗ, после того как ты зарегистрировался на сайте
-- под своим ником.
--
-- Роль по умолчанию — участник, и назначить администратора может только
-- администратор. Первого поэтому назначают руками, отсюда этот файл.
-- Если бы роль выдавалась по просьбе, её мог бы попросить кто угодно.
--
-- ЧТО ПОПРАВИТЬ: в строке ниже замени ТВОЙ_НИК на свой ник — тот, который
-- ты придумал при регистрации на форуме. С логином GitHub он не связан
-- никак и совпадать не обязан.
--
-- ЧТО ДОЛЖНО ПОЛУЧИТЬСЯ: «Success. 1 row(s) affected».
--
-- Если написало «0 rows» — ник не совпал. Посмотри точное написание
-- запросом в самом низу этого файла и подставь оттуда. Регистр не важен,
-- лишний пробел или другая буква — важны.

update public.forum_users
   set role = 'admin'
 where lower(nick) = lower('ТВОЙ_НИК');


-- После этого на сайте надо выйти и войти снова: роль читается при входе,
-- в уже открытой сессии она останется прежней.


-- ── Если что-то не сходится ─────────────────────────────────────────────────
--
-- Посмотреть, кто вообще есть в базе и с какими ролями. Выдели строку ниже
-- и нажми Run — так запустится только она.

-- select nick, role, created_at from public.forum_users order by created_at;
